export enum Gender {
  male = 1,
  female = 2,
}
export enum Role {
  manager = 'manager',
  runner = 'runner',
}

export enum WEEK {
  Mon = '월',
  Tue = '화',
  Wed = '수',
  Thu = '목',
  Fri = '금',
  Sat = '토',
  Sun = '일',
}

export enum InputType {
  text = 'text',
  password = 'password',
}
